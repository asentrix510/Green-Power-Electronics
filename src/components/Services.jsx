import { motion } from "framer-motion";
import { Cpu, Sun, Battery, Zap, Wrench } from "lucide-react";

export default function Services() {
  return null; // This component is no longer needed as services are now in Products
}